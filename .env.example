# ============================================================================
# AutoSpendTracker Configuration
# ============================================================================

# ----------------------------------------------------------------------------
# Google Cloud Project Configuration
# ----------------------------------------------------------------------------
PROJECT_ID=your-google-cloud-project-id
# Gemini 3 Flash preview uses the global endpoint (set LOCATION=global)
LOCATION=us-central1

# Service Account Configuration
SERVICE_ACCOUNT_FILE=ASTservice.json

# ----------------------------------------------------------------------------
# Google Sheets Configuration
# ----------------------------------------------------------------------------
SPREADSHEET_ID=your-google-sheets-spreadsheet-id
SHEET_RANGE=Sheet1!A2:G

# ----------------------------------------------------------------------------
# Application Settings
# ----------------------------------------------------------------------------
LOG_LEVEL=INFO
OUTPUT_FILE=transaction_data.json

# OAuth Token Storage
# TOKEN_DIR defaults to ~/.autospendtracker/secrets
# Uncomment to customize:
# TOKEN_DIR=/path/to/custom/token/directory

# ----------------------------------------------------------------------------
# Email Processing Configuration
# ----------------------------------------------------------------------------
# Number of days to look back for emails (default: 7 for weekly analysis)
EMAIL_DAYS_BACK=7

# Gmail label for marking processed emails
GMAIL_LABEL_NAME=AutoSpendTracker/Processed

# ----------------------------------------------------------------------------
# AI Model Configuration
# ----------------------------------------------------------------------------
# Model name (gemini-3-flash-preview, gemini-2.0-flash, etc.)
MODEL_NAME=gemini-3-flash-preview

# Model temperature for generation (0.0 = deterministic, 2.0 = creative)
MODEL_TEMPERATURE=0.1

# ----------------------------------------------------------------------------
# API Rate Limiting (New in Modernization)
# ----------------------------------------------------------------------------
# Maximum number of API calls per period
API_RATE_LIMIT_CALLS=60

# Rate limit period in seconds (default: 60 = per minute)
API_RATE_LIMIT_PERIOD=60

# ----------------------------------------------------------------------------
# Performance Monitoring (New in Modernization)
# ----------------------------------------------------------------------------
# Enable performance monitoring and metrics collection
ENABLE_PERFORMANCE_MONITORING=true

# Log level for metrics output (DEBUG, INFO, WARNING, ERROR, CRITICAL)
METRICS_LOG_LEVEL=INFO

# ----------------------------------------------------------------------------
# Cost Budget Tracking (Optional)
# ----------------------------------------------------------------------------
# Uncomment to enable daily budget tracking
# DAILY_BUDGET=5.00
# BUDGET_ALERT_THRESHOLD=0.8

# ============================================================================
# Notes:
# ============================================================================
# 1. All settings are validated using Pydantic Settings
# 2. Required settings: PROJECT_ID, SPREADSHEET_ID
# 3. For detailed documentation, see: docs/MODERNIZATION.md
# 4. Performance monitoring provides automatic cost tracking
# 5. Rate limiting prevents API quota exhaustion
# ============================================================================
